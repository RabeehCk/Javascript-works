// Array Methods

// push(obj) -> add element at the end
// pop()     -> remove element at the end
// unshift(obj) -> add element at the start
// shift()   -> remove element at the start
// include(obj) -> check if element is present in the aaray



var colors = ["red","blue","green"];
// colors.push("yellow")
// colors.pop()
// colors.unshift("yellow")
// colors.shift();
console.log(colors.includes("Blue"));